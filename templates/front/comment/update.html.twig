{% extends "/base.html.twig" %}

{% block body %}
	{% set user = user() %}
	<article class="d-flex flex-column justify-content-center">
		<h1 class="text-center">Modifies ton Bubbles Comment</h1>
		<h5 class="text-center">Tu veux revoir ta copie ? <br> C'est ici que ça se passe !</h5>
		<a class="btn my-4" href="/comment/comment_user/{{ user.slug }}">Retour</a>

		<section class="border rounded p-3">
			<form action="/comment/update/{{ comment.id }}" method="post" class="d-flex flex-column p-4 mx-1 mx-md-4">
				<textarea class="form-control border border-2" name="content" id="Content" rows="6" value="{{ comment.content }}">{{ comment.content }}</textarea>
				<label for="Contenu" class="form-label h6">Contenu</label>
	
				<button class="btn" type="submit">Modifier</button>
			</form>
		</section>
	</article>
{% endblock %}
