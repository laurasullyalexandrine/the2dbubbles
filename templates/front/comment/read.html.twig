{% extends "/base.html.twig" %}

{% block body %}
	{# <article class="d-flex flex-column justify-content-center">
			<div class="text-center mb-4">
				<h1>Bienvenue dans ton <br>
					<span class="marker">Bubbles Space</span>!
				</h1>
				<h5>C'est ici que tu gères tes
					<span class="marker">Bubbles Comment</span>.</h5>
			</div>
			{% if comments|length == 0 %}
				<div class="d-flex flex-column text-center my-5">
					<h6 class="fs-6">Tu n'as pas encore de
						<span class="marker">Bubbles Comment</span>!</h6>
					<p class="fs-5">A toi de jouer!</p>
				</div>
			{% else %}
				<section class="d-flex flex-wrap border rounded p-3">
					{% for comment in comments %}
						<div class="card mb-3" style="width: 20rem; min-height: 12rem;">
							<div class="card-body">
	
								<h6 class="card-title text-center">
									{% if comment.status == 1 %}
										Bubbles Comment validé 
										<i class="fa-solid fa-face-smile" style="color: #17cf1a;"></i>
									{% elseif comment.status == 2 %}
									Bubbles Comment en attente 
										<i class="fa-solid fa-circle-question" style="color: #eda507;"></i>
									{% elseif comment.status == 0 %}
									Bubbles Comment rejeté 
										<i class="fa-solid fa-circle-xmark" style="color: #ed072a;"></i>
									{% endif %}
								</h6>
								<p class="card-text fw-bold">
									<br>
									{{ comment.content }}
	
								</p>
								<h6 class="card-subtitle mb-2 text-muted">
									Titre Bubbless Post :
									{{comment.post}}
									<br>
									Auteur :
									{{ author.pseudo }}
									<br>
									Publié le :
									<time datetime="{{ post.createdAt|date('d-m-Y') }}">{{ post.createdAt|date('d-m-Y') }}</time>
									<time datetime="{{ post.createdAt|date('H:i:s') }}">{{ post.createdAt|date('H:i:s') }}</time>
								</h6>
								<div class="d-flex justify-content-center mt-4">
									<a class="bluegrey me-3" href="/comment/update/{{ comment.id }}" class="card-link">Modifier</a>
									{{ include('front/comment/_delete.html.twig') }}
								</div>
							</div>
						</div>
					</section>
				{% endfor %}
			{% endif %}
		</article> #}

	<article class="row d-flex flex-wrap justify-content-between">
		<div class="col-lg-5 mt-lg-4">
			<img src="/image/space.png" alt="image-space" class="w-100">
		</div>
		<section class="col p-3 d-flex flex-wrap">
			<div class="mb-4">
				<h1>
					Bienvenue dans ton
					<br>
					Bubbles Space!
				</h1>
				<h6>C'est ici que tu gères tes Bubbles Comment.</h6>
			</div>
			{% if comments|length == 0 %}
				<div class="d-flex flex-column text-center my-5">
					<h6 class="fs-6">Tu n'as pas encore de
						<span class="marker">Bubbles Comment</span>!</h6>
					<p class="fs-5">A toi de jouer!</p>
				</div>
			{% else %}
				{% for comment in comments %}
					<div class="d-flex flex-wrap justify-content-center mb-3">
						<div class="card shadow-sm bg-body rounded me-3" style="width: 18rem; height: 12rem;">
							<div class="card-body flex-wrap">
								<h6 class="card-title">
									{% if comment.status == 1 %}
										Bubbles Comment validé
										<i class="fa-solid fa-face-smile" style="color: #17cf1a;"></i>
									{% elseif comment.status == 2 %}
										Bubbles Comment en attente
										<i class="fa-solid fa-circle-question" style="color: #eda507;"></i>
									{% elseif comment.status == 0 %}
										Bubbles Comment rejeté
										<i class="fa-solid fa-circle-xmark" style="color: #ed072a;"></i>
									{% endif %}
								</h6>
								<p class="card-text">{{ comment.content }}</p>
								<ul class="list-group">
									<li class="fs-6 list-group-item">
										Titre Bubbles Post :
										{{comment.post}}
										<br>
									Auteur :
									{{ author.pseudo }}
										Publié le :
										<time datetime="{{ post.createdAt|date('d-m-Y') }}">{{ post.createdAt|date('d-m-Y') }}</time>
										<time datetime="{{ post.createdAt|date('H:i:s') }}">{{ post.createdAt|date('H:i:s') }}</time>
									</li>
								</ul>
								<div class="d-flex">
									<a class="card-link" href="/comment/update/{{ comment.id }}">Modifier</a>
									{{ include('front/comment/_delete.html.twig') }}
								</div>
							</div>
						</div>
					</div>
				{% endfor %}
			{% endif %}
		</section>
	</article>
{% endblock %}
